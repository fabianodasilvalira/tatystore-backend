version: "3.9"

services:
  db:
    container_name: tatystore-backend-db
    image: postgres:14
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: tatystore
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - tatystore-network

  api:
    container_name: tatystore-backend-api
    build:
      context: .
      dockerfile: Dockerfile
    image: tatystore-backend:latest
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/tatystore
      ALEMBIC_DATABASE_URL: postgresql://postgres:postgres@db:5432/tatystore
      SECRET_KEY: supersecret-change-in-production
      DEBUG: "false"
      PROJECT_NAME: "TatyStore Backend"
      VERSION: "1.0.0"

      # IMPORTANTE — URL usada para gerar o link da imagem
      API_BASE_URL: http://localhost:8080
      UPLOAD_DIR: /app/uploads

    volumes:
      - ./uploads:/app/uploads     # <--- agora persistindo e visível no host

    ports:
      - "8080:8080"                # <--- movido para o lugar correto

    networks:
      - tatystore-network

volumes:
  pg_data:
  uploads:

networks:
  tatystore-network:
    driver: bridge
